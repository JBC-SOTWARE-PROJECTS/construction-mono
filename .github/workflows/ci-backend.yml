# name: CONSTRUCTION FRONT BETA

# on: 
#   push:
#      branches: [ main ]
# jobs:
#   build:
#     runs-on: self-hosted 
 
#     steps:
#     - uses: actions/checkout@v2
#     - name: Welcome Message
#       run:  echo CICD Test Runner
  
#     - name: Login to Docker Hub
#       uses: docker/login-action@v2
#       with:
#         username: ${{ secrets.DOCKER_HUB_USERNAME }}
#         password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
#     - name: build jar-file 
#       run: |
#        sudo /opt/gradle/bin/gradle build
#     - name: docker build 
#       run: |
#        docker build -f backend/Dockerfile ./backend -t acemcbmis/private-backend-demo:0.0.1
#     - name: docker push
#       run: |
#         docker push acemcbmis/private-backend-demo:0.0.1

# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2
      - name: Welcome Message
        run:  echo CICD Test Runner



      - name: Build Docker
        run: |
           
            PROJ_VERSION=$(sudo /opt/gradle/bin/gradle properties --no-daemon --console=plain -q | grep "^version:" | awk '{printf $2}')
            echo "Project is ${PROJ_VERSION}"
            sudo /opt/gradle/bin/gradle build
            docker build -t "acemcbmis/private-backend-demo:${PROJ_VERSION}" --build-arg PROJ_VERSION="${PROJ_VERSION}" .
            docker login -u acemcbmis -p p@ssword@@c3mc
            docker push "acemcbmis/private-backend-demo:${PROJ_VERSION}"