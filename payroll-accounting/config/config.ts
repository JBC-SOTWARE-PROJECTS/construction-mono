// config/config.ts
// eslint-disable-next-line import/no-anonymous-default-export
export default {
  locale: {
    default: 'en-US', // Set the default locale to 'en-US' or any other language code you prefer.
    antd: true,
    baseNavigator: true, // Enables auto-switching based on browser language settings.
  },
}
